<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!-- Declarar que la cámara es opcional -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />


    <application
        android:theme="@style/AppTheme"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="31">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.cambiosformatokuka.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>



        <!-- LoginActivity como actividad principal -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- GuiaActivity -->
        <activity
            android:name=".GuiaActivity"
            android:exported="true" />

        <!-- ManualActivity -->
        <activity
            android:name=".ManualActivity"
            android:exported="true" />

        <!-- PasoActivity -->
        <activity
            android:name=".PasoActivity"
            android:exported="true" />

        <!-- AgregarManualActivity -->
        <activity
            android:name=".AgregarManualActivity"
            android:exported="true" />

        <!-- AgregarGuiaActivity -->
        <activity
            android:name=".AgregarGuiaActivity"
            android:exported="true" />

        <!-- VerManualActivity -->
        <activity
            android:name=".VerManualActivity"
            android:exported="true" />

        <activity
            android:name=".AgregarPasoActivity"
            android:exported="true" />

        <activity
            android:name=".ImagenCompletaActivity"
            android:exported="true"/>



    </application>

</manifest>
